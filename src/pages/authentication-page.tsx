import { PagePaddingWrapper } from '../common';
import React from 'react';
import { AuthenticationPageContainer } from '../modules/users';

export const AuthenticationPage: React.FC = () => {
  return (
    <AuthenticationPageContainer/>
  );
};
